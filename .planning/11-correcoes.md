# 11 — Correções apuradas nos reviews interno (09) e externo (10)

Dezesseis correções aplicadas na ordem A0 → A15. Princípio: **contradição →
corrige · ausência → declara**. Nenhum requisito, número, política ou endpoint
novo foi criado; onde a reunião não decidiu, o documento passou a dizer que não
decidiu. Nenhum arquivo de `src/`, `prisma/`, `tests/` ou configuração foi
tocado — INV-1 conferido a cada rodada.

Estado do verificador: **34/34 antes**, oscilou para 33/34 uma vez durante A6
(INV-7, corrigido no mesmo passo), **34/34 no fim**.

| ID | O que mudou | Arquivo:linha | Fonte do achado | Comando de prova |
|---|---|---|---|---|
| A0 | Reclassificação de `Fonte = CODIGO`. Saíram por origem genérica (`http-errors.ts:27`, classe `NotFoundError`, que serve a qualquer 404): FDD-ERR-04 → `TRANSCRICAO [09:28] Bruno` (a fala nomeia `WEBHOOK_NOT_FOUND` literalmente), FDD-ERR-05 → `CODIGO customer.service.ts:25` (origem específica de verdade), FDD-ERR-08 → §Itens sem origem. Saíram por origem transcrita: ADR-006 → `[09:30] Larissa`, ADR-007 → `[09:41] Diego`. Entraram: FDD-CONTRATO-01..05 (a reunião deu o verbo, não a forma do path) e PRD-RNF-20 (`redactPaths`). Acrescentada a §Critério de classificação de `Fonte = CODIGO` | `docs/TRACKER.md`:32,59–64,67–69,80–81,144–170; `docs/PRD.md`:162 | 09 §6, §8.2 | `grep -cF '[09:28] Bruno' TRANSCRICAO.md` → 2 · `grep -cF '[09:30] Larissa' TRANSCRICAO.md` → 1 · `grep -cF '[09:41] Diego' TRANSCRICAO.md` → 2 · `grep -n "NotFoundError('Customer')" src/modules/customers/customer.service.ts` → 25 · `./scripts/verify.sh \| grep TRK-4` → 8 linhas CODIGO |
| A1 | "8 transições" → **7**; "5 das 8" → **4 de 7**; "4 das 8" → **4 de 7** | `docs/adrs/ADR-007-…`:25 e :133; `docs/FDD.md`:126 | 09 §6 nota D | `sed -n '4,9p' src/modules/orders/order.status.ts \| sed -E 's/^[^:]*://' \| grep -o 'OrderStatus\.[A-Z]*' \| wc -l` → **7** (PENDING 2 · PAID 2 · PROCESSING 2 · SHIPPED 1 · DELIVERED 0 · CANCELLED 0). Tocam estoque 3 (`shouldDebitStock` :29–31 · `shouldReplenishStock` :33–37) → 4 intocadas. Envolvem PENDING/CANCELLED: 4 |
| A2 | Aritmética do retry fixada em **5 chamadas / 4 intervalos 1m·5m·30m·2h / última tentativa em 2h36min**, marcada `(interpretação — a ata é ambígua, ver RFC-QA-05)`. Diagrama, tabela de instantes, tabela de números e critério de aceite realinhados; ADR-003 §Decisão reescrita citando `[09:17] Larissa`; bullet novo em §Consequências/Negativas registrando as três falas incompatíveis e a margem de ~36 min sobre a indisponibilidade de 2h; RFC-QA-05 criada | `docs/FDD.md`:170–195, :45, :594–596, :763–764; `docs/adrs/ADR-003-…`:29–36, :91–93, :113–124; `docs/PRD.md`:150–151; `docs/RFC.md`:174; `docs/TRACKER.md`:36–37,60 | 09 §6 nota B | `python3 -c "print(1+5+30+120)"` → **156 min = 2h36** · margem sobre 120 min = **36 min** · `grep -nE '^\[09:1[5-8]\]' TRANSCRICAO.md` (as três falas: Larissa "5 tentativas", Diego "1m/5m/30m/2h/12h", Diego "quase 15 horas") |
| A3 | Contradição DLQ resolvida pela leitura do FDD: a linha de origem **é retida e marcada `FAILED`**, não removida. ADR-003 deixou de afirmar remoção e passou a registrar a retenção como decisão do desenho, não da ata (a ata decidiu só a tabela separada, DEC-06/`[09:18] Bruno`) | `docs/adrs/ADR-003-…`:34–36, :91–93, :117–123 | 10 §6 item 12 | `grep -n 'sai da outbox' docs/adrs/ADR-003-*.md` → vazio · `grep -n 'marcada como `FAILED`' docs/FDD.md` → §DLQ e replay |
| A4 | Replay passou a **preservar o `event_id` original**; §Mapeamento registra que `event_id` é o id do evento e não necessariamente o id da linha; implicação registrada em ADR-005 §Consequências/Negativas | `docs/FDD.md`:212–219, :559, :700–705; `docs/adrs/ADR-005-…`:87–94 | 10 §6 item 3 | `grep -n 'preserva o `event_id`' docs/FDD.md` · `grep -n 'id do evento, não necessariamente o id da linha' docs/FDD.md` |
| A5 | "latência aceita no pior caso: 2s" → "**latência mínima imposta pelo intervalo de polling: 2s**". A linha de §Objetivos que prometia 2s como teto foi **retirada** — o sistema não a cumpre por construção | `docs/PRD.md`:146 (RNF-03) e §Objetivos (linha removida); `docs/FDD.md`:600; `docs/TRACKER.md`:32 | 09 §6 nota A | `grep -nF '[09:10] Larissa' TRANSCRICAO.md` → "A latência **mínima** vai ser 2 segundos no pior caso" · `grep -rn 'pior caso' docs/PRD.md docs/FDD.md` → só citações literais da ata |
| A6 | "50 pedidos/min" rebaixado de requisito/objetivo a **cenário citado**. Linha de §Objetivos retirada; PRD-RNF-16 reescrito declarando que nada foi dimensionado nem prometido; tracker alinhado | `docs/PRD.md`:158 e §Objetivos (linha removida); `docs/TRACKER.md`:45 | 09 §6 nota C | `grep -nF '[09:38] Diego' TRANSCRICAO.md` → "**Se** o cliente tem 50 pedidos mudando de status em um minuto, a gente bombardeia ele com 50 chamadas?" (pergunta retórica que abre o assunto tirado de escopo em `[09:39]`) |
| A7 | Documentada a colisão entre DEC-13 (prefixo `WEBHOOK_`) e as classes base existentes. §Matriz de erros ganhou o parágrafo que distingue as três classes que aceitam código por parâmetro das três que o fixam, e as 5 linhas afetadas ganharam marcador **†**; §Integração ganhou o delta real em `http-errors.ts` e a linha nova de `validate.middleware.ts`; ADR-006 §Consequências/Negativas registra que DEC-13 exige alterar classes base — trabalho declarado, não escondido. **Nenhum código alterado** | `docs/FDD.md`:571–581, :583–595, :869–870; `docs/adrs/ADR-006-…`:§Negativas | 10 §6 item 5 | `grep -n "super(.*'VALIDATION_ERROR'\|super(.*'FORBIDDEN'\|super(.*'NOT_FOUND'" src/shared/errors/http-errors.ts` → 11, 23, 29 (sem parâmetro de código) · `grep -n readonly src/shared/errors/app-error.ts` → 5 `errorCode` · `cat -n src/middlewares/validate.middleware.ts` → 25–32 todo `ZodError` vira `ValidationError` |
| A8 | Registrado que `TxClient` é alias **local** e não exportado: ou passa a ser exportado (delta no arquivo existente) ou a assinatura usa `Prisma.TransactionClient` direto | `docs/FDD.md`:832–840 | 10 §6 item 7 | `grep -n 'TxClient' src/modules/orders/order.service.ts` → 24 (`type TxClient = …`, sem `export`), usos em 205, 234, 245 · `grep -rn 'export type TxClient' src/` → vazio |
| A9 | Nomeado o delta que fecha a rota aninhada: os três símbolos de registro (`buildCustomerRouter`, a chamada em `routes/index.ts`:26 e o tipo `Controllers`) mudam juntos | `docs/FDD.md`:874–875 | 10 §6 item 6 | `cat -n src/modules/customers/customer.routes.ts` → 12 `buildCustomerRouter(controller: CustomerController)`, único parâmetro · `cat -n src/routes/index.ts` → 13 `Controllers`, 26 montagem de `/customers` |
| A10 | Declarado que a propagação do `requestId` é **delta novo** em controller e service; §Tracing deixou de prometer correlação que não existe | `docs/FDD.md`:841–853, :709–716 | 10 §6 item 7 | `awk 'NR>=5&&NR<=8' src/middlewares/request-logger.middleware.ts` → gera `req.id` · `awk 'NR>=126&&NR<=130' src/modules/orders/order.service.ts` → `changeStatus(id, input, userId)` · `awk 'NR>=38&&NR<=42' src/modules/orders/order.controller.ts` → não repassa `req.id` |
| A11 | FDD-ERR-08 deixou de mandar conferir o id "na consulta da fila": o texto agora declara que **nenhum dos 7 contratos expõe listagem da DLQ** e que a lacuna não foi decidida na reunião. **Endpoint não criado** | `docs/FDD.md`:590 | 10 §6 item 13 | `grep -c '^### ' <(sed -n '/^## Contratos públicos/,/^## /p' docs/FDD.md)` → 7 blocos, nenhum de listagem de dead-letter |
| A12 | Origem falsa primeiro: FDD-ERR-06 saiu da tabela principal para §Itens sem origem (a fala institui o grace period e não recusa re-rotação; bloquear a re-rotação impede revogar secret comprometida por 24h). FDD-ERR-03, -06, -09, -12 e -13 marcados no FDD com `(decisão de desenho deste FDD, sem origem na reunião)`, na matriz e no bloco de status da rotação | `docs/FDD.md`:585,588,591,594,595, :424–429; `docs/TRACKER.md`:70 (removida) e §Itens sem origem | 09 §2 item 12, §8.1 | `grep -nF '[09:21] Sofia' TRANSCRICAO.md` → institui as 24h, nada sobre recusar nova rotação · `grep -niE '2xx' TRANSCRICAO.md` → vazio (FDD-ERR-12 sem lastro) · `grep -rniE 'hmac\|crypto\|createHmac\|signature' src/ prisma/ tests/ package.json` → vazio |
| A13 | As três afirmações de grau de requisito sem ID ganharam ID e linha de tracker: **PRD-RNF-20** (`redactPaths`, Fonte CODIGO), **PRD-RNF-21** (prazo de novembro, `[09:45] Marcos`) e **PRD-RNF-22** (secret nunca exposta fora de criação/rotação → §Itens sem origem, porque a fala cobre só a devolução na criação). A promessa de "critério de aceite próprio" do 5º risco passou a ter o critério correspondente | `docs/PRD.md`:27,76,162–164,202–205,196; `docs/TRACKER.md`:49–50 e §Itens sem origem | 09 §3, §7 (TRK-2) | `grep -nF '[09:45] Marcos' TRANSCRICAO.md` → "A Atlas quer pra fim de novembro" · `grep -nF '[09:31] Marcos' TRANSCRICAO.md` → "secret é gerada pela gente e devolvida na criação" (não proíbe as demais consultas) · `cat -n src/shared/logger/index.ts` → 4–11, seis entradas, sem secret |
| A14 | "todos os doze padrões" com dez enumerados → os **doze** enumerados (faltavam `fila/broker` e `publisher/emissão`). Descrição da migration corrigida: não há statement `CREATE INDEX`; os índices são cláusulas `INDEX` dentro de `CREATE TABLE`. O argumento (não há trigger) não foi enfraquecido | `docs/FDD.md`:7–11; `docs/adrs/ADR-001-…`:84–88 | 09 §6 notas E e F | `awk '/^## 4/{f=1} f && /^## 5/{f=0} f' .planning/02-codigo.md \| grep -E '^\| *`' \| wc -l` → **12** · `grep -cE '^CREATE INDEX' prisma/migrations/20260519182739_init/migration.sql` → **0** · `CREATE TABLE` → 7 · `ALTER TABLE` → 6 · `wc -l` → 125 |
| A15 | Fronteira entre documentos: saíram do RFC o handle transacional (duas ocorrências) e o bloco de `snake_case` × `camelCase` / exceção de id; o TL;DR ganhou a ressalva de DIV-08. DIV-08 ficou em três alturas — problema de produto no PRD (intacto), **uma linha** de ressalva no RFC, referência a ADR-008 no ADR-001 | `docs/RFC.md`:21–23, :40–47, :72–74, :185–186, :202–205; `docs/adrs/ADR-001-…`:17–23 | 09 §5.1, §5.4 | `wc -w docs/RFC.md` → **2055** (teto 2200) · `./scripts/verify.sh \| grep RFC-6` → 0 linhas de detalhe de FDD |

## Não aplicado

Nada da lista A0–A15 ficou por aplicar.

Fora da lista e **deliberadamente não feito**, porque é ausência de decisão e não
contradição — matéria do próximo bloco, não deste:

- endpoint de listagem da dead-letter queue (A11 declara a lacuna, não a fecha);
- lease/recuperação do estado `PROCESSING` do worker (10 §6 itens 1 e 4);
- política de lifecycle do `DELETE` de endpoint (10 §6 item 9);
- contrato de segurança de saída — SSRF, redirects, key management (10 §6 item 10);
- caminho de produção do worker e das métricas (10 §6 item 11);
- dono e data das cinco regras marcadas `(decisão de desenho deste FDD, sem
  origem na reunião)` e das duas linhas novas de §Itens sem origem — a decisão
  editorial "remove ou assume" não é do corretor;
- reescrita do `README.md` (09 §9 item 11), planejada para o bloco 9.
