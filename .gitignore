node_modules/
dist/
coverage/
.env
.env.local
.env.*.local
*.log
.DS_Store
.idea/
.vscode/
*.tsbuildinfo

# ── Artefatos de processo ──
# A entrega é o pacote de documentos; o rastro de produção (notas de bloco e o
# verificador executável dos critérios de aceite) não é documento entregue.
# Ficou versionado durante a produção e saiu do índice no fechamento, mas segue
# em disco e preservado no histórico. 07f8496 é o último commit em que o
# verificador esteve versionado; a árvore de lá não é o pacote final. Ver
# README.md §Como navegar a entrega.
.planning/
scripts/
