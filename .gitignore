node_modules/
dist/
coverage/
.env
.env.local
.env.*.local
*.log
.DS_Store
.idea/
.vscode/
*.tsbuildinfo

# ── Override do ~/.gitignore_global (linha 12: .planning/) ──
# Artefatos de processo desta entrega precisam ser versionados.
!.planning/

# Artefatos de processo — fora da entrega, preservados no histórico
.planning/
scripts/
