node_modules/
dist/
coverage/
.env
.env.local
.env.*.local
*.log
.DS_Store
.idea/
.vscode/
*.tsbuildinfo

# ── Artefatos de processo ──
# A entrega é o pacote de documentos; o rastro de produção (notas de bloco e o
# verificador executável dos critérios de aceite) não é documento entregue.
# Ficou versionado durante a produção e saiu do índice no fechamento, mas segue
# preservado no histórico — reproduzível a partir do commit 07f8496.
.planning/
scripts/
